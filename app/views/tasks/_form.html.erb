<div class="container">
  <%= simple_form_for(@task) do |t| %>
    <%= t.input :title %>
    <%= t.input :details %>
    <%= t.input :completed %>
    <%= t.submit "Save", class: "btn btn-primary" %>
  <% end %>
</div>
